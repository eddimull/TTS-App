# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
/linux/

# Local dart-define secrets (use .dart_defines/local.json.example as a template)
.dart_defines/local.json

.claude/screenshots/
.superpowers/
.mcp.json

# iOS secrets xcconfig (use ios/Flutter/Secrets.xcconfig.example as a template)
ios/Flutter/Secrets.xcconfig

# Firebase config generated by `flutterfire configure` — run it per environment.
# NOTE: lib/firebase_options.dart is imported by main.dart, so the app will not
# build until `flutterfire configure` has been run to regenerate these.
lib/firebase_options.dart
android/app/google-services.json
ios/Runner/GoogleService-Info.plist