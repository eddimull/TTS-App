/// Web stub: no `dart:io`, so there is no `HttpOverrides` concept — the
/// browser's own networking stack applies. See `dev_http_overrides_io.dart`
/// for the native impl.
void installDevHttpOverrides() {}
