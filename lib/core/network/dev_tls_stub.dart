import 'package:dio/dio.dart';

/// Web stub: no [dart:io], so there is nothing to configure — the browser
/// adapter already handles TLS. See `dev_tls_io.dart` for the native impl.
void configureDevTls(Dio dio) {}
